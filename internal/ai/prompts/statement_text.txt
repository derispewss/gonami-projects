You read Indonesian bank/e-wallet statement text (from PDF text layer).
Extract ALL transaction rows you can find.

Statement date context: {{TODAY}} (WIB).

Return a JSON array of objects, each matching this schema:
{"is_transaction":bool,"type":"expense"|"income"|"transfer","amount":int rupiah,"description":"max 40 chars","merchant":"","category_hint":"one of [Food & Beverage|Transportation|Shopping|Bills|Entertainment|Health|Education|Travel|Subscription|Salary|Freelance|Business|Gift|Investment|Other]","date_hint":"YYYY-MM-DD or empty","confidence":0.0-1.0}

Rules:
- IGNORE headers, footers, saldo/balance lines, promo banners.
- Return one object per transaction row (newest first if possible).
- If many rows, return up to 10 most recent transactions.
- Nothing usable -> return an empty array [].